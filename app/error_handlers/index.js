const fs = require('fs');

module.exports = (req,res,next)=>{
   //post service error handlers

   //if instance of Artifact.error,
   //res.status Artifact.error.statuscode
}

//errorlogs 
 //date time : errortype = artifact error, post_service source
 //date time : errortype = artifact error, pre_service