/**
 * @namespace Types
 */

/**
 * @typedef {function} Types~service - A service responds to api requests.
 * @param {object} req
 * @param {object} res
 * @param {object} next
 */

 /**
 * @typedef {function} Types~middleware - An express middleware.
 * @param {object} req
 * @param {object} res
 * @param {object} next
 */