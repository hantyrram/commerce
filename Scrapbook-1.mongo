db.employees.aggregate([
   {
      $match : {
         "userAccount": {"$exists": true}
      }
   },
   {
      "$replaceWith": { $mergeObjects: [{ _id: "$_id", credential:null, roles: null},"$userAccount"]}
   }
])